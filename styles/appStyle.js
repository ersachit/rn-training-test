import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    height: 40,
    margin: 15,
    borderWidth: 1,
    padding: 10,
    borderColor: 'black',
    borderRadius: 5,
  },
  textStyle: {
      fontSize: 30
  }
});
